u32 test(u32* ptr, u1 cond) {
entry:
    one   = u32 1
    two   = u32 2
    three = u32 3

    store u32* ptr, u32 one
    bcond u1 cond, on_true, on_false

on_true:
    store u32* ptr, u32 two
    branch exit

on_false:
    branch exit

exit:
    ret u32 three
}
