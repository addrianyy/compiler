u32 test(u32* ptr) {
entry:
    val_1 = load u32, u32* ptr
    one_1 = u32 1
    res_1 = add u32 val_1, one_1
    store u32* ptr, u32 res_1

    val_2 = load u32, u32* ptr
    one_2 = u32 1
    res_2 = add u32 val_2, one_2
    store u32* ptr, u32 res_2

    return_value = load u32, u32* ptr
    ret u32 return_value
}
